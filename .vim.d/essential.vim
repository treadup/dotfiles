" To run vim without using a config file use the following command.
" vim -u NONE

" To run as vim instead of vi use the following command.
" vim -N

" To run as vim without a config file use the following command.
" vim -u NONE -N

" This is a minimal vim configuration
set nocompatible
filetype plugin on
